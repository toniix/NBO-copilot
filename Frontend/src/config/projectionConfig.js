// Projection configuration for supervisor projections
export const PROJECTION_WINDOW_DAYS = 14 // use last N days to compute daily average
export const MONTH_DAYS = 30

export default { PROJECTION_WINDOW_DAYS, MONTH_DAYS }
